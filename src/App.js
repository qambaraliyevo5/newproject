import './App.css';
import { BrowserRouter, Route, Routes } from 'react-router-dom';

function App() {
  <BrowserRouter>
  <Routes>
    <Route path='/admin' element={<Admin_Layout/>}/>
  </Routes>
  </BrowserRouter>
}

export default App;
